const axios = require('axios');
const run = async () => {
  try {
    // Attempting a bad ID to trigger the stack trace locally in script
    // Or I can just check the backend.log or error logs
  } catch(e) {}
};
